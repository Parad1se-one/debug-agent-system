name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run offline test subset (no proprietary data required)
        run: |
          PYTHONPATH=src python3 scripts/ci_offline_tests.py

      - name: CLI smoke
        run: |
          PYTHONPATH=src python3 -m debug_agent_system.adapters.cli diagnose \
            "AOI主程序初始化失败，相机连接异常，请检查相机IP" --non-interactive | head -5
